2 for loops

Use a regular variable for keeping track of the channel IDs.

Make sure to keep track of the current category id number to be used as the parent_id for the channels.

Change structure style for the template yaml to specify channel type for each channel listed.

Use dataclass to create channel and role objects, and use asdict function/method to convert them into a usable format for the JSON.



