2 for loops - done

Use a regular variable for keeping track of the channel IDs. - done

Make sure to keep track of the current category id number to be used as the parent_id for the channels. - done

Change structure style for the template yaml to specify channel type for each channel listed. - done

Use dataclass to create channel and role objects, and use asdict function/method to convert them into a usable format for the JSON. - not needed

For roles parameters:

id works the same way channels do
name same
color integer representation of hexadecimal color code
hoist boolean
position integer - higher number means higher priority
permissions 

Possibly only need id and name


Add field for name in the template.  

Make additional function for outputting the completed payload.


possibly just needs name for role


Specify for the template layout that all categories need at least one channel for configuration purposes.
